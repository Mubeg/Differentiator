# 1 "commands.h"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 1 "<command-line>" 2
# 1 "commands.h"
# 9 "commands.h"
DEF_DIF(cst, MODE_CNST, '\0' , return node_create_new(MODE_CNST, 0, nullptr, nullptr);)

DEF_DIF(var, MODE_VARL, '\0' , return node_create_new(MODE_CNST, 1, nullptr, nullptr);)

DEF_DIF(add, MODE_FUNC, '+', return node_create_new(MODE_FUNC, '+', do_diff_recursive(node->left), do_diff_recursive(node->right));)

DEF_DIF(sub, MODE_FUNC, '-', return node_create_new(MODE_FUNC, '-', do_diff_recursive(node->left), do_diff_recursive(node->right));)

DEF_DIF(mul, MODE_FUNC, '*', return node_create_new(MODE_FUNC, '+', node_create_new(MODE_FUNC, '*', do_diff_recursive(node->left), node_copy(node->right)), node_create_new(MODE_FUNC, '*', node_copy(node->left), do_diff_recursive(node->right)));)
